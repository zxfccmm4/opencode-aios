# Project Context

> Fill this file when copying AIOS into a new project.
> It is the fastest way for an agent to orient itself. Keep it short and factual.

## One-Line Summary

What this project is, in one sentence.

## Tech Stack

- Language:
- Framework:
- Runtime / version:
- Package manager:
- Database:
- Deployment target:

## Run Commands

```bash
# install
# dev / run
# build
# test
# lint / typecheck
```

## Directory Layout

```text
.
├── path/to/source
├── path/to/tests
└── ...
```

Highlight any non-obvious folders (generated code, vendored libs, monorepo packages).

## Key Modules

List the 3–7 modules an agent must understand to do most tasks. One line each.

- `path/to/module` — what it does

## Environment

Variables, config files, or secrets the project expects. Do not put values here, only names.

## Gotchas

Non-obvious behaviors an agent should know up front. Examples: generated files that should not be edited by hand, services that must be running, platforms not supported.
